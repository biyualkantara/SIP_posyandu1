clone dlu atau download rar terus cara install container docker, jalankan ini: buka dlu file docker-compose.yml di terimalnya: docker compose up -d
lalu nanti jangan lupa npm run dev

setelah berhasil
lalu di terminal nya juga jalankan npm run dev
